server:
  port: ${PORT:8084}

spring:
  application:
    name: transaction-service
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://kim:alinemongo%401@cluster0.0jrqyub.mongodb.net/transaction_db}
  rabbitmq:
    addresses: ${SPRING_RABBITMQ_ADDRESSES:amqp://guest:guest@localhost:5672}

account-service:
  url: ${ACCOUNT_SERVICE_URL:http://localhost:8083}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html